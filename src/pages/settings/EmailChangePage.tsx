const EmailChangePage = () => {
    return (
        <div className="flex min-h-screen items-center justify-center">
            <h1 className="text-4xl font-bold">이메일 변경 페이지</h1>
        </div>
    );
};

export default EmailChangePage;
